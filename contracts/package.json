{
  "name": "contracts",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run script/deploy.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "hardhat": "^2.22.18",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "viem": "^2.21.53",
    "dotenv": "^16.4.5"
  }
}