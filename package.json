{
  "name": "prividium-addresses",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter web dev",
    "test": "pnpm --filter contracts test",
    "dev:services": "pnpm --parallel --filter resolver --filter relayer-l1 --filter relayer-l2 dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
